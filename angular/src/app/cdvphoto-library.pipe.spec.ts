import { CDVPhotoLibraryPipe } from './cdvphoto-library.pipe';

describe('CDVPhotoLibraryPipe', () => {
  it('create an instance', () => {
    const pipe = new CDVPhotoLibraryPipe();
    expect(pipe).toBeTruthy();
  });
});
